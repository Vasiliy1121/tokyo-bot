from openai import OpenAI
import re
from config import OPENAI_API_KEY

openai_client = OpenAI(api_key=OPENAI_API_KEY)

async def generate_itinerary(data):
    prompt = f"""
    Ты — эксперт по туризму и профессиональный гид в Японии, специализирующийся исключительно на создании качественных, уникальных и очень подробных маршрутов по Токио.

    Пользователь запросил персональный маршрут с такими параметрами:

    1. Длительность поездки: {data['days']} дней.
    2. Состав путешественников: {data['travelers']}.
    3. Основные интересы: {data['interests']}.
    4. Темп путешествия: {data['pace']}.
    5. Уровень бюджета: {data['budget']}.
    6. Предпочтения по еде: {data['food']}.
    7. Дополнительные пожелания или ограничения: {data['special_requests']}.

    Пожалуйста, составь максимально подробный, логичный и интересный маршрут по следующим строгим критериям:

    - 📌 **Структура дня:**  
      Каждый день должен быть разделён на три логичные части:
      - 🌅 **Утро** (примерно 8:00–12:00) — достопримечательности или места, которые лучше посещать утром.
      - 🏙️ **День** (примерно 12:00–17:00) — основные активности, прогулки и развлечения.
      - 🌃 **Вечер** (примерно 17:00–22:00) — места для вечерних прогулок, развлечений, ужина и отдыха.

    - 🗺️ **Конкретные рекомендации:**  
      Для каждого временного блока указывай точные и конкретные достопримечательности, районы или мероприятия (музеи, парки, рестораны, торговые центры и пр.), которые идеально вписываются в указанные интересы.

    - 🚉 **Логистика и транспорт:**  
      Указывай удобный способ передвижения между местами (метро с указанием линий, автобусы, пешком или такси). Учитывай минимальное время на переезды и не допускай нелогичных перемещений по городу.

    - 🍱 **Рекомендации по питанию:**  
      Предлагай конкретные рестораны, кафе или заведения, учитывая предпочтения по еде и уровень бюджета. Рекомендуй типичные японские блюда, которые стоит попробовать.

    - 🌤️ **Сезонность и актуальность:**  
      Учитывай, что поездка состоится в июне 2025 года. Предлагай мероприятия и места, которые особенно интересны именно в это время года (погода, сезонные события и мероприятия в городе).

    - 👥 **Состав путешественников:**  
      Тщательно учитывай специфику состава группы, например, если это семья с детьми — добавь больше подходящих детских мест и активностей, если это пара — больше романтических мест.

    - 💡 **Дополнительные пожелания:**  
      Максимально подробно учитывай указанные особые пожелания или ограничения, чтобы маршрут был максимально персонализированным.

    Форматируй ответ очень чётко, структурируя его по дням:
    - Каждый день начинай с заголовка «📅 День X».
    - Внутри дня разделяй на блоки «🌅 Утро», «🏙️ День», «🌃 Вечер».
    - Названия мест и заведений пиши на английском языке
    - Все остальное пиши на русском языке.

    Маршрут должен быть максимально увлекательным, подробным и полезным.  
    """

    response = openai_client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[{"role": "user", "content": prompt}],
        temperature=0.7,
        max_tokens=3000
    )

    itinerary = response.choices[0].message.content.strip()
    itinerary_with_links = add_google_maps_links(itinerary)
    return itinerary_with_links


async def edit_day(current_itinerary, day_number, user_request):
    day_pattern = rf"(📅?\s*День\s*{day_number}.*?)(?=(📅?\s*День\s*\d+)|\Z)"
    match = re.search(day_pattern, current_itinerary, re.DOTALL)

    if not match:
        raise ValueError(f"Не найден День {day_number}")

    day_text = match.group(1).strip()

    prompt = f"""
        Ты — эксперт по туризму в Токио. Вот текущий вариант Дня {day_number} маршрута:

        {day_text}

        Пользователь просит изменить этот день следующим образом:
        "{user_request}"

        Создай полностью новый вариант Дня {day_number}, учтя пожелания пользователя.
        Используй четкую структуру (📅 День X, 🌅 Утро, 🏙️ День, 🌃 Вечер).
        """

    response = openai_client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[{"role": "user", "content": prompt}],
        temperature=0.7,
        max_tokens=1500
    )

    edited_day_text = response.choices[0].message.content.strip()
    new_itinerary = current_itinerary.replace(day_text, edited_day_text)

    return new_itinerary





import re

def add_google_maps_links(itinerary_text):
    def replace_link(match):
        action, place = match.groups()
        query = place.strip().replace(' ', '+')
        link = f"[{place.strip()}](https://www.google.com/maps/search/?api=1&query={query})"
        return f"- {action} {link}"

    pattern = re.compile(
        r"-\s*(Посетите|Посещение|Экскурсия по|Экскурсия в|Прогулка по|Ужин в|Завтрак в|Обед в|Посетить|Проведите утро в)\s+([^\.,\n]+)"
    )
    return pattern.sub(replace_link, itinerary_text)





